# 이 레포의 목적
- HCI 논문의 프로토타입·실험을 재현하며 시스템 구현을 배우는 학습 레포. 제품이 아니다.
- 사용자(정웅)는 초심자다. 코드 품질보다 "이해 가능성"을 우선한다.

# 폴더 규칙
- 루프마다 loops/NN-slug/ 폴더 하나. 그 안에 README.md, EXPLAIN.md, QUIZ.md, microworld.html, 소스 코드.
- docs/ 폴더는 GitHub Pages용. 마이크로월드 html은 docs/NN-slug.html 로도 복사해 둔다.

# 코딩 규칙
- 언어: Python 우선. 웹 UI가 필요하면 Streamlit. 브라우저 실험(반응시간 측정 등)은 순수 HTML+JS 단일 파일.
- 외부 라이브러리는 최소화하고, 쓰면 왜 쓰는지 주석으로 남긴다.
- 파일 하나가 200줄을 넘기면 나눈다. 초심자가 한 화면에서 읽을 수 있어야 한다.
- 한국어 주석. 함수마다 한 줄 docstring.

# 설명 규칙 (내가 "설명서"를 요청하면)
- 순서 고정: (1) 전체 구조와 데이터 흐름 → (2) 파일·함수별 한 줄 요약 → (3) 실행 순서대로 흐름 서술 → (4) 코드는 맨 마지막.
- 결과는 EXPLAIN.md 파일로 저장한다. 채팅에는 저장했다는 말만 한다.

# 변형 단계 규칙
- 내가 "변형 시작"이라고 하면, 그 이후로 내가 명시적으로 요청하기 전까지 코드를 대신 고치지 않는다.
- 막혔다고 하면 정답 코드가 아니라 힌트(어느 파일의 어느 함수를 볼지, 어떤 개념인지)만 준다.
- 내가 "정답 줘"라고 명시하면 그때 준다.

# 절대 금지
- 군 관련 정보를 파일이나 대화에 남기지 않는다.
- hci-lab 폴더 바깥의 파일을 읽거나 수정하지 않는다.
